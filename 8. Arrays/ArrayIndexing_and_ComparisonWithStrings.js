let str = "Aman";
str[0] = "B",
    console.log(str); //Immutable

let arr = [1, 2, 3, 4, 5];
arr[3] = 90;
console.log(arr); //Mutable

//Difference bewteem array and string is that
/**
 * arrays are mutable
 * strings are immutable
 */